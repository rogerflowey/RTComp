[package]
name = "rt"
version = "0.1.0"
edition = "2021"
description = "Facade re-exporting rt_attr's RT-annotation attribute macros as the `rt::` namespace."

[lib]
proc-macro = false

[dependencies]
rt_attr = { path = "../rt_attr" }